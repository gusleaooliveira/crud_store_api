const express = require('express');
const router = express.Router();
const controller = require('../controller/controllerPacote');


router.get('/', );
router.get('/search', );
router.get('/:id', );
router.put('/:id', );
router.delete('/:id', );
router.post('/', );

module.exports = router;